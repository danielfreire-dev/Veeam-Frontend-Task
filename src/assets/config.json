{
	"form": { "title": "This is the Result Form!" },
	"items": [
		{
			"label": "numeric",
			"type": "number",
			"name": "numeric"
		},
		{
			"label": "string",
			"type": "text",
			"name": "string"
		},
		{
			"label": "multi-line",
			"type": "text-area",
			"name": "multi-line"
		},
		{
			"label": "boolean",
			"type": "checkbox",
			"name": "boolean"
		},
		{
			"label": "enum",
			"type": "radio",
			"name": "enum"
		},
		{
			"label": "date",
			"type": "date",
			"name": "date"
		}
	],
	"buttons": [
		{ "text": "Ok", "type": "button" },
		{ "text": "Save", "type": "submit" },
		{ "text": "Cancel", "type": "reset" }
	]
}
